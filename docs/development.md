# Development

Use Node 24 and Corepack. Run `pnpm install`, then `pnpm verify`. `pnpm dev:extension` starts WXT development; `pnpm dev:native-host` runs the host in stdio mode. Automated tests use mock mode and no API key.
